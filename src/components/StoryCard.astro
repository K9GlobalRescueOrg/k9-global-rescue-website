---
const { title, text, href = "/rescue-stories/", tag = "Rescue story" } = Astro.props;
---

<article class="story-card">
  <div class="story-image" aria-hidden="true"></div>
  <div class="story-copy">
    <span>{tag}</span>
    <h3>{title}</h3>
    <p>{text}</p>
    <a href={href}>Read story</a>
  </div>
</article>

<style>
  .story-card {
    overflow: hidden;
    border: 1px solid var(--color-border);
    border-radius: var(--radius);
    background: #090a07;
    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.32);
    color: var(--color-ivory);
  }

  .story-image {
    min-height: 132px;
    background:
      linear-gradient(135deg, rgba(0, 0, 0, 0.84), rgba(232, 180, 72, 0.2)),
      url("https://images.unsplash.com/photo-1530281700549-e82e7bf110d6?auto=format&fit=crop&w=900&q=80");
    background-position: center;
    background-size: cover;
  }

  .story-copy {
    padding: 1.25rem;
  }

  .story-copy span {
    display: inline-block;
    margin-bottom: 0.75rem;
    color: var(--color-accent);
    font-size: 0.75rem;
    font-weight: 900;
    text-transform: uppercase;
  }

  .story-copy p {
    color: var(--color-ink-soft);
    margin-bottom: 0.75rem;
  }

  .story-copy a {
    color: var(--color-accent);
    font-weight: 900;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }
</style>
